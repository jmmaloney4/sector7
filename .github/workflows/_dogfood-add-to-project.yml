name: 📌 add to project (dogfood)
on:
  issues:
    types: [opened, reopened, transferred, labeled]
  pull_request:
    types: [opened, reopened, labeled]
permissions:
  contents: read
jobs:
  add-to-project:
    # Secrets cannot be used in `if`; set PROJECT_URL when you want this enabled.
    if: vars.PROJECT_URL != ''
    uses: jmmaloney4/sector7/.github/workflows/add-to-project.yml@03cf2aa947933ae54fbeb7f3ec08c81525f75921 # main
    with:
      project_url: ${{ vars.PROJECT_URL }}
    secrets:
      github-token: ${{ secrets.PROJECT_PAT }}
